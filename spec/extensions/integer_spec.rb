require 'spec_helper'

describe Integer do

  it "should take the number 0 and map it to the letter 'a'" do
    0.to_alphabet.should eq 'a'
  end

  it "should take the number 1 and map it to the letter 'b'" do
    1.to_alphabet.should eq 'b'
  end

  it "should take the number 2 and map it to the letter 'c'" do
    2.to_alphabet.should eq 'c'
  end

  it "should take the number 3 and map it to the letter 'd'" do
    3.to_alphabet.should eq 'd'
  end

  it "should take the number 4 and map it to the letter 'e'" do
    4.to_alphabet.should eq 'e'
  end

  it "should take the number 5 and map it to the letter 'f'" do
    5.to_alphabet.should eq 'f'
  end

  it "should take the number 6 and map it to the letter 'g'" do
    6.to_alphabet.should eq 'g'
  end

  it "should take the number 7 and map it to the letter 'h'" do
    7.to_alphabet.should eq 'h'
  end

  it "should take the number 8 and map it to the letter 'i'" do
    8.to_alphabet.should eq 'i'
  end

  it "should take the number 9 and map it to the letter 'j'" do
    9.to_alphabet.should eq 'j'
  end

  it "should take the number 10 and map it to the letter 'k'" do
    10.to_alphabet.should eq 'k'
  end

  it "should take the number 11 and map it to the letter 'l'" do
    11.to_alphabet.should eq 'l'
  end

  it "should take the number 12 and map it to the letter 'm'" do
    12.to_alphabet.should eq 'm'
  end

  it "should take the number 13 and map it to the letter 'n'" do
    13.to_alphabet.should eq 'n'
  end

  it "should take the number 14 and map it to the letter 'o'" do
    14.to_alphabet.should eq 'o'
  end

  it "should take the number 15 and map it to the letter 'p'" do
    15.to_alphabet.should eq 'p'
  end

  it "should take the number 16 and map it to the letter 'q'" do
    16.to_alphabet.should eq 'q'
  end

  it "should take the number 17 and map it to the letter 'r'" do
    17.to_alphabet.should eq 'r'
  end

  it "should take the number 18 and map it to the letter 's'" do
    18.to_alphabet.should eq 's'
  end

  it "should take the number 19 and map it to the letter 't'" do
    19.to_alphabet.should eq 't'
  end

  it "should take the number 20 and map it to the letter 'u'" do
    20.to_alphabet.should eq 'u'
  end

  it "should take the number 21 and map it to the letter 'v'" do
    21.to_alphabet.should eq 'v'
  end

  it "should take the number 22 and map it to the letter 'w'" do
    22.to_alphabet.should eq 'w'
  end

  it "should take the number 23 and map it to the letter 'x'" do
    23.to_alphabet.should eq 'x'
  end

  it "should take the number 24 and map it to the letter 'y'" do
    24.to_alphabet.should eq 'y'
  end

  it "should take the number 25 and map it to the letter 'z'" do
    25.to_alphabet.should eq 'z'
  end

  it "should take the number 26 and map it to the letter 'ba'" do
    26.to_alphabet.should eq 'ba'
  end

  it "should take the number 27 and map it to the letter 'bb'" do
    27.to_alphabet.should eq 'bb'
  end

  it "should take the number 28 and map it to the letter 'bc'" do
    28.to_alphabet.should eq 'bc'
  end

  it "should take the number 29 and map it to the letter 'bd'" do
    29.to_alphabet.should eq 'bd'
  end

  it "should take the number 30 and map it to the letter 'be'" do
    30.to_alphabet.should eq 'be'
  end

  it "should take the number 31 and map it to the letter 'bf'" do
    31.to_alphabet.should eq 'bf'
  end

  it "should take the number 32 and map it to the letter 'bg'" do
    32.to_alphabet.should eq 'bg'
  end

  it "should take the number 33 and map it to the letter 'bh'" do
    33.to_alphabet.should eq 'bh'
  end

  it "should take the number 34 and map it to the letter 'bi'" do
    34.to_alphabet.should eq 'bi'
  end

  it "should take the number 35 and map it to the letter 'bj'" do
    35.to_alphabet.should eq 'bj'
  end

  it "should take the number 36 and map it to the letter 'bk'" do
    36.to_alphabet.should eq 'bk'
  end

  it "should take the number 37 and map it to the letter 'bl'" do
    37.to_alphabet.should eq 'bl'
  end

  it "should take the number 38 and map it to the letter 'bm'" do
    38.to_alphabet.should eq 'bm'
  end

  it "should take the number 39 and map it to the letter 'bn'" do
    39.to_alphabet.should eq 'bn'
  end

  it "should take the number 40 and map it to the letter 'bo'" do
    40.to_alphabet.should eq 'bo'
  end

  it "should take the number 41 and map it to the letter 'bp'" do
    41.to_alphabet.should eq 'bp'
  end

  it "should take the number 42 and map it to the letter 'bq'" do
    42.to_alphabet.should eq 'bq'
  end

  it "should take the number 43 and map it to the letter 'br'" do
    43.to_alphabet.should eq 'br'
  end

  it "should take the number 44 and map it to the letter 'bs'" do
    44.to_alphabet.should eq 'bs'
  end

  it "should take the number 45 and map it to the letter 'bt'" do
    45.to_alphabet.should eq 'bt'
  end

  it "should take the number 46 and map it to the letter 'bu'" do
    46.to_alphabet.should eq 'bu'
  end

  it "should take the number 47 and map it to the letter 'bv'" do
    47.to_alphabet.should eq 'bv'
  end

  it "should take the number 48 and map it to the letter 'bw'" do
    48.to_alphabet.should eq 'bw'
  end

  it "should take the number 49 and map it to the letter 'bx'" do
    49.to_alphabet.should eq 'bx'
  end

  it "should take the number 50 and map it to the letter 'by'" do
    50.to_alphabet.should eq 'by'
  end

  it "should take the number 51 and map it to the letter 'bz'" do
    51.to_alphabet.should eq 'bz'
  end

end